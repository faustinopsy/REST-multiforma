// Model.php
<?php
class Model {
    protected function find($id) {
        // Simulação: buscar no banco de dados pelo ID
    }

    protected function findAll() {
        // Simulação: buscar todos os registros no banco de dados
    }

    protected function create($data) {
        // Simulação: criar um registro no banco de dados
    }

    protected function update($id, $data) {
        // Simulação: atualizar um registro no banco de dados pelo ID
    }

    protected function delete($id) {
        // Simulação: deletar um registro no banco de dados pelo ID
    }
}
